import React from 'react';
import type { IconProps } from '../types';

export const ApplePay24 = ({
  color = 'currentColor',
  className,
  style,
}: IconProps) => (
  <svg viewBox="0 0 36 24" width={36} height={24}
       fill={color} className={className} style={style} aria-hidden="true" focusable="false">
    <rect width="35.0222" height="24"/>
  </svg>
);
