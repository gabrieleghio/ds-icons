import React from 'react';
import type { IconProps } from '../types';

export const ApplePay48 = ({
  color = 'currentColor',
  className,
  style,
}: IconProps) => (
  <svg viewBox="0 0 71 48" width={71} height={48}
       fill={color} className={className} style={style} aria-hidden="true" focusable="false">
    <rect width="70.0444" height="48"/>
  </svg>
);
